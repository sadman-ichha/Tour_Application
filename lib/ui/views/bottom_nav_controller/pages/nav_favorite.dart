import 'package:flutter/material.dart';

class NavFavorite extends StatelessWidget {
  const NavFavorite({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}